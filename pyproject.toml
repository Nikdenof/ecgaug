[project]
name = "ecgaug"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.1",
]

[dependency-groups]
dev = [
    "neurokit2>=0.2.13",
]
